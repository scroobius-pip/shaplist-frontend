import { Button } from 'evergreen-ui'
import React from 'react'


const SocialLoginButton = () => {
    return <Button onClick={()=>{}} iconBefore={<img/>} size='large'>Facebook</Button>
}

export default SocialLoginButton